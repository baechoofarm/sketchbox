import React from "react";
import {Meta} from "@storybook/react";

export default {
    title: 'Sketchbox/Test'
} as Meta;

const Template = () => {
    return 'TEST';
};

export const Primary = Template.bind({});
